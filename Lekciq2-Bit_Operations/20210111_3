#include <stdio.h>

int bit(int x, int pos);
int main(){
    int x = 7;
    int pos = 1;
    printf("%d\n", bit(x, pos));

}

int bit(int x, int pos){
    x = x & (~(1 << pos));
    

    return x;


}
